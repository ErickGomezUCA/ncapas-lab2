spring:
  application:
    name: labo_dos_n_capass
  datasource:
    url: jdbc:postgresql://localhost:5432/labo_2
    username: postgres
    password: 1l0v3c0d3

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    defer-datasource-initialization: true
  sql:
    init:
      mode: always